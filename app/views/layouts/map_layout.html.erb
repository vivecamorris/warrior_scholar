<!DOCTYPE html>
<html>
  <head>
    <meta name="viewport" content="initial-scale=1.0, user-scalable=no" />
    <style type="text/css">
      html { height: 100% }
      body { height: 100%; margin: 0; padding: 0 }
      #map-canvas { height: 100% }
    </style>
    <script
      type="text/javascript"
      src="https://maps.googleapis.com/maps/api/js?key=AIzaSyCkuiyOmS0FikjETYC8UhQMt7dsVa8dV08">
    </script>
  </head>
  <body>
    <%= yield %>
    <script type="text/javascript">

      function initialize() {

        var myLatlng = new google.maps.LatLng(40, -80);

        var mapOptions = {
          // these two lines set the zoom and center point of the map object
          zoom: 4,
          center: myLatlng,
          streetViewControl: false,
          overviewMapControl: false,
          mapTypeControl: false,
          zoomControlOptions: {
            style: google.maps.ZoomControlStyle.DEFAULT,
          }
        };

        var map = new google.maps.Map(document.getElementById('map-canvas'), mapOptions);

        // Adding info window and content to markers

        var contentString = '<div id="content">'+
              '<div id="siteNotice">'+
              '</div>'+
              '<h1 id="firstHeading" class="firstHeading">Rob Henderson</h1>'+
              '<div id="bodyContent">'+
              '<p><b>Rob Henderson</b>, one of the 2014 Yale Warrior Scholars, is a <b>seven year veteran of the Air Force</b> based at Ramstein Air Base in Germany. Before becoming a Warrior-Scholar, he had resigned himself to taking classes online from public colleges:' +
              '</br></br><em>[The Warrior-Scholar Project] raised my expectation for myself. It made me think, &lsquo;Maybe I can go to a school like this.&rsquo; </em></p>'+
              '</div>'+
              '</div>';

      var infowindow = new google.maps.InfoWindow({
      content: contentString
      });

      // To add the marker to the map, use the 'map' property
      var marker = new google.maps.Marker({
      position: myLatlng,
      map: map,
      animation: google.maps.Animation.DROP,
      title:"Hello World!"
      });

      google.maps.event.addListener(marker, 'click', function() {
      infowindow.open(map,marker);
      });

    }

      initialize();
    </script>
  </body>
</html>